#include<stdio.h>
int main()
{
    int n,res;
    printf("enter the value of n ");
    scanf("%d",&n);
    res=fact(n);
    printf("result %d ",res);
}
int fact(int a)
{
    int res ;
    if(a==0)
    res=1;
    else
    res=a*fact(a-1);
    return res;
}
